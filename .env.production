# For production: Set to empty to use relative URLs that get proxied by Vercel
# The proxy/rewrite configuration is in vercel.json
# Leave empty to make relative API calls (e.g., /api/v1/...) that Vercel will proxy to the backend
VITE_API_URL=
VITE_IMAGES_BASE_URL=https://storage.googleapis.com/plantopia-images-1757656642/plant_images
